/* path: app/hamburger/doctor/[id]/layout.tsx */
import type { ReactNode } from 'react';

export const viewport = {
  themeColor: '#f6f7fb',
};

export default function Layout({ children }: { children: ReactNode }) {
  return children;
}
