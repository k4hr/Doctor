/* path: app/hamburger/profile/admin/doctor/[id]/layout.tsx */
import type { ReactNode } from 'react';

export const viewport = {
  themeColor: '#ffffff',
};

export default function Layout({ children }: { children: ReactNode }) {
  return children;
}
